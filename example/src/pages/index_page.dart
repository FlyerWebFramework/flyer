import 'package:flyer/widgets.dart';

class IndexPage extends WebPage {
  const IndexPage();

  @override
  Widget build() {
    return Column(
      children: [
        Text("Header"),
        Title("Nadpis"),
      ],
    );
  }
}
